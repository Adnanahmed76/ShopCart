//List of enums
//thet cannot be created inside a class

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentsMethods {
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  creditCard
}
